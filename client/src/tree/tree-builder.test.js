// @flow

//import TreeBuilder from './tree-builder';

describe('reducer', () => {
  test('addNode', () => {
    expect(true).toBe(true);
  });

  test('deleteNode', () => {
    expect(true).toBe(true);
  });
});
